import { contractSpyType } from 'example-app/helpers/contract-spy-type';
import { module, test } from 'qunit';

module('Unit | Helper | contract spy type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = contractSpyType([42]);
  assert.ok(result);
});
